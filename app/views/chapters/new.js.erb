var $bookdiv = $('div.book[data-id="<%= @book.id %>"]');
$formNewChapter = $bookdiv.find(".formNewChapter");
$formNewChapter.html("<%= escape_javascript(render 'chapterForm', book: @book) %>");
$formNewChapter.show();