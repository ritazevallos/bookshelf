var $categorydiv = $('div.categorydiv[data-id="<%= @category.id %>"]');
var $categoryHeader = $categorydiv.find('.categoryHeader');

$categoryHeader.html("<%= escape_javascript(render 'formnewcategory', category: @category) %>")