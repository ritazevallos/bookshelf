var $bookdiv = $('div.book[data-id="<%= @book.id %>"]');
$chaptersDiv = $bookdiv.find(".chapterList");
$chaptersDiv.html("<%= escape_javascript(render 'bookChapters', book: @book) %>");
$chaptersDiv.show();