alert('if this message appears, that means update is finally working yay!!');

$("ul.errors").html("")
<% if @book.errors.any? %>
	<% @book.errors.full_messages.each do |message| %>
		$("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
	<% end %>
<% else %>
	$("#category_list").html("<%= escape_javascript(render 'shared/list', categories: @categories) %>")
	$(.editform).hide()
	$(.content).show()
<% end %>//todo: why the hell is this not being executed? angryface